---
description: Quick headless WordPress scan - fast pattern detection for WPGraphQL schema extensions, frontend queries, preview mode, and revalidation/webhook risk
argument-hint: "[path]"
---

Use the **wp-headless-and-wpgraphql** skill to perform a quick headless WordPress check.

**Target**: $ARGUMENTS (if empty, use current working directory)

Focus only on the "Search Patterns for Quick Detection" section -- run the scan commands to find WPGraphQL, headless frontend, and preview/cache issues fast. Report matches with file:line references and severity levels. Skip deep analysis.

If critical issues are found, suggest running `/wp-headless-review` for comprehensive headless architecture review with safer alternatives.
