---
description: Quick WordPress REST API scan - fast pattern detection for route registration, permission callbacks, raw input handling, and response issues
argument-hint: "[path]"
---

Use the **wp-rest-api-development** skill to perform a quick REST API check.

**Target**: $ARGUMENTS (if empty, use current working directory)

Focus only on the "Search Patterns for Quick Detection" section -- run the scan commands to find REST API issues fast. Report matches with file:line references and severity levels. Skip deep analysis.

If critical issues are found, suggest running `/wp-rest-review` for comprehensive REST API review with fix recommendations.

